info:
  name: GET_ALL_REGISTERS
  type: http
  seq: 16

http:
  method: GET
  url: "{{PROTO}}{{HOST}}:{{PORT}}/api/{{V2}}/register_processing/view/registers"
  auth: inherit

runtime:
  variables:
    - name: V2
      value: v2

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
