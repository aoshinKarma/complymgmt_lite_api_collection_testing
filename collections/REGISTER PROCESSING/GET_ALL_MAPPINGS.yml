info:
  name: GET_ALL_MAPPINGS
  type: http
  seq: 12

http:
  method: GET
  url: "{{PROTO}}{{HOST}}:{{PORT}}/api/{{V2}}/register_processing/view/mappings"
  auth: inherit

runtime:
  variables:
    - name: V2
      value: v2

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
