info:
  name: GET_REGISTER_BY_ID
  type: http
  seq: 17

http:
  method: GET
  url: "{{PROTO}}{{HOST}}:{{PORT}}/api/{{V2}}/register_processing/view/register/343567890ytu"
  auth: inherit

runtime:
  variables:
    - name: V2
      value: v2

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
