meta {
  name: GET_TOTAL_EMPLOYEE_COUNT
  type: http
  seq: 4
}

get {
  url: {{PROTO}}{{HOST}}:{{PORT}}/api/{{VERSION}}/dashboard/view/payroll/total/employee/count
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}
